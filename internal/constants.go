package internal

import (
	"fmt"
)

const Text = "text"
const Number = 2
const Boolean = true
const Float = 3.45

func Constants() {
	fmt.Println("Constante do tipo string: ", Text, "Constante do tipo int: ", Number, "Constante do tipo bool: ", Boolean, "Constante do tipo float: ", Float)
}
